```
# 起動
$ docker compose up -d

# 実行
$ docker compose exec python python main.py

# コンテナに入る
$ docker compose exec python bash

# 一時停止
$ docker compose stop

# 終了
$ docker compose down
```

```
# Swagger UI
http://localhost:8000/docs
```